import {b64} from "./sdcwt.js";
const $=id=>document.getElementById(id),LABELS={title:"Title",component:"Component",severity:"Severity",fingerprint:"Fingerprint",patch:"Patch",patch_date:"Patch date"};let token;
async function post(path,body){const response=await fetch(path,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)}),data=await response.json();if(!response.ok)throw new Error(data.detail||response.statusText);return data}
function field(label,value){const row=document.createElement("div");row.className="field-row";const name=document.createElement("label");name.textContent=label;const content=value==null?document.createElement("span"):document.createElement("code");if(value==null)content.className="redacted-value";else content.textContent=value;row.append(name,content);return row}
function renderReport(report){$("report-subject").textContent=report?.subject||"Untrusted disclosure";$("report-txid").textContent=report?.txid||"Not verified";$("field-list").replaceChildren();for(const [name,label] of Object.entries(LABELS))$("field-list").append(field(label,report?.fields?.[name]));$("body-preview").replaceChildren();if(report?.body?.known)for(const chunk of report.body.chunks){const span=document.createElement("span");if(chunk==null){span.className="redacted";span.textContent="██████"}else span.textContent=chunk;$("body-preview").append(span)}else{const span=document.createElement("span");span.className="redacted";span.textContent="████████████";$("body-preview").append(span)}$("references").replaceChildren();if(Array.isArray(report?.references)&&report.references.length)for(const value of report.references){const row=document.createElement("div");row.className="reference";row.textContent=value;$("references").append(row)}else $("references").append(field("",null).lastChild)}
function renderChecks(result){$("checks").replaceChildren();for(const check of result.checks){const row=document.createElement("div");row.className=`check ${check.status}`;const icon=document.createElement("i");icon.dataset.lucide=check.status==="pass"?"circle-check":"circle-alert";const copy=document.createElement("div"),title=document.createElement("strong"),detail=document.createElement("small");title.textContent=check.name;detail.textContent=check.detail;copy.append(title,detail);row.append(icon,copy);$("checks").append(row)}$("overall").className=`overall ${result.valid?"pass":"fail"}`;$("overall-title").textContent=result.valid?"Disclosure verified":"Verification failed";const limited=result.checks.some(check=>check.status==="unavailable");$("overall-detail").textContent=result.valid?(limited?"Valid under the prototype trust model":"All checks passed"):"Do not rely on disclosed contents";lucide.createIcons()}
async function load(file){token=new Uint8Array(await file.arrayBuffer());$("file-title").textContent=file.name;$("file-detail").textContent=`${token.length.toLocaleString()} B`;$("verify").disabled=false;$("results").hidden=true;$("verify-error").textContent=""}
$("token-file").onchange=event=>load(event.target.files[0]);for(const event of ["dragenter","dragover"])$("drop-zone").addEventListener(event,()=>$("drop-zone").classList.add("dragging"));for(const event of ["dragleave","drop"])$("drop-zone").addEventListener(event,()=>$("drop-zone").classList.remove("dragging"));$("drop-zone").ondrop=event=>{event.preventDefault();load(event.dataTransfer.files[0])};$("drop-zone").ondragover=event=>event.preventDefault();
$("verify-form").onsubmit=async event=>{event.preventDefault();try{$("verify").disabled=true;$("verify-error").textContent="";const result=await post("/api/verify",{token:b64(token),audience:$("audience").value});renderReport(result.report);renderChecks(result);$("verify-form").hidden=true;$("results").hidden=false}catch(error){$("verify-error").textContent=error.message}finally{$("verify").disabled=false}};$("audience").oninput=()=>{$("results").hidden=true};$("verify-another").onclick=()=>{$("results").hidden=true;$("verify-form").hidden=false;$("token-file").value="";token=undefined;$("verify").disabled=true;$("file-title").textContent="Choose a signed disclosure";$("file-detail").textContent="Load a `.kbt.cose` file"};lucide.createIcons();